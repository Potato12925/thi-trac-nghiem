# Repository Rules

- Use Hibernate ORM
- Use SessionFactory
- Use HQL when possible
- Use LAZY loading
- Repository handles DB access only

Avoid:

- Raw JDBC
- Spring Data JPA auto repositories
- Business logic
